export namespace IEntity {
  // export interface ICategory {}
  export interface IProducts {
    id: number;
    title: string;
    description: string;
    price: number;
    brand: string;
    category: string;
    thumbnail: string;
  }
}
